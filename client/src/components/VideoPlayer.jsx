import React from 'react';
import { Grid, Typegraphy, Paper } from '@material-ui/core';
import { makeStyles } from '@material-ui/core';

import { SocketContext } from '../SocketContext';

const VideoPlayer = () => {
    return(
        <div>
           Vidéo player
        </div>
    )
}

export default VideoPlayer;